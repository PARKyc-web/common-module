spring:
  application:
    name:comm-module

  datasource:
    url: jdbc:postgresql://localhost:5432/postgre
    username: postgre
    password: postgre
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true